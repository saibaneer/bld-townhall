# Lucy's journey — spec §15.2, exercised end to end with no telecom and no LLM.
#
# The request line is STRUCTURED, not natural language: M6's own gate says
# "without ... LLM", and M11 owns the verbatim natural-language line (the
# ScriptedProposer's grammar stands where the model will).
#
# M7C makes it approve-first (ADR-026): BOOK raises a challenge and books
# NOTHING; the preview carries a random code; the booking happens only after
# Lucy replies YES with that code. The runner captures the code from the preview
# and substitutes it into `YES {code}` — a script cannot hardcode a random PIN.

> +447700900123 BOOK date=2026-09-10 from=14:00 to=17:00 people=20 accessible=yes max=5000
< Maximum booking fee: £50.00

> +447700900123 YES {code}
< Booked. Council ref

> +447700900123 STATUS
< Booked. Attendees 20. Council ref

# Twenty minutes later (spec §15.2's beat) — the referent resolves from the
# authoritative cancellable set, not from anything the session remembered, and
# the cancellation presents the grant the booking's own approval issued.
> +447700900123 cancel it
< Cancelled. Council ref
