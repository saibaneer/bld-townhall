# Adversarial tests

The deterministic hostile-proposer suite becomes a first-class integration target as public service surfaces are added. Domain-level impossible/denied cases start in `townhall-domain` unit tests.
